# Webex-Launcher-Auto Beta

## Per usare la Beta
* Scarica la repository;
* Apri la cartella Win64;
* Trova il file `.exe` e aprilo (se vuoi crea un collegamento sul desktop facendo `invia a`, `Desktop (crea collegamento)`.
